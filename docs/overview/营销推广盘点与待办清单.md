 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |# 📋 灵犀AI英语听说 · 营销推广盘点与待办清单
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |> 生成时间：2026-08-18  
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |> 基于文档：docs/marketing/（58个MD + 42个PDF）  
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |> 状态：执行中
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 一、当前进度总览
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 模块 | 状态 | 完成度 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||------|------|--------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || B端激活码系统 | ✅ 已上线 | 100% |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 邀请裂变（前端） | ✅ 已完成 | 100% |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 邀请裂变（后端） | ✅ 已验证API可用 | 100%（待端到端测试） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || C端推广方案文档 | ✅ 齐全 | 100% |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || B端推广方案文档 | ✅ 齐全 | 100% |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 专家团 | ❌ 未启动 | 0%（计划Week 7，已超期） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 代理商体系 | ✅ 方案设计 | 0%（无落地执行） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 社群运营 | ❌ 未启动 | 0%（SOP文档完备，群未建立） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 内容营销 | ❌ 未启动 | 0%（30天日历完备，无实际发布） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || ASO优化 | ❌ 未启动 | 0%（指南完备，App Store未上架） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 数据埋点 | 🚧 进行中 | 70%（权限快照代码完成，手动验证待完成） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 二、技术阻塞项（必须优先处理）
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### 🔴 P0 - 阻塞性Bug
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |#### 问题1：Android离线ASR结束录音闪退
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **根因**：sherpa-onnx Silero VAD native 推理部分机型崩溃
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **已尝试方案**：cpu provider、AudioRecord串行、自适应跳过VAD
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **阻塞影响**：Android端核心跟读功能不可用，影响所有Android用户
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **下一步**：必须拿到真机 logcat + `/data/tombstones` 定位
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **行动**：找3-5台不同品牌Android真机复现并抓取崩溃日志
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |#### 问题2：段落复述未复用统一录音识别模块
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **位置**：`lib/providers/repeat_flow/` vs `lib/providers/listen_and_repeat/`
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **影响**：体验割裂，代码重复维护
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |- **状态**：待实现
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 三、App端待开发功能清单
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### P1 - 高优先级
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 功能 | 描述 | 预期价值 | 代码位置 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|------|----------|----------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 3 | 邀请裂变端到端测试 | 验证后端闭环 | 裂变获客核心通道 | `lib/features/subscription/services/invite_service.dart` |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 4 | 启动埋点手动验证PostHog | 确认权限快照/用户属性落库 | 数据驱动基础 | `lib/analytics/` |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 5 | 学习Tab直接进学习页 | 跳过学习计划页 | 缩短路径，提升留存 | `lib/router/app_router.dart` |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 6 | 学习Tab「今日完成任务」折叠区 | 增强目标感 | DAU/留存 | `lib/providers/learning_session/` |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### P2 - 增长与变现
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 功能 | 描述 | 预期价值 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|------|----------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 7 | 句子复制能力 | 移动端长按、桌面端右键 | 分享传播 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 8 | 自定义背景/背景音 | 个性化学习体验 | 付费转化 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 9 | 播放完成音效+任务完成动画 | 正向反馈激励 | 留存/参与度 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 10 | 学习耗时预计/实际对比展示 | 帮助用户规划 | 留存 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 11 | 埋点按中国大陆/全球拆分落地 | PostHog双环境 | 合规+精准分析 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### 营销基础设施
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 功能 | 描述 | 预期价值 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|------|----------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 12 | App Store/Google Play提交审核 | ASO指南已完成 | 自然流量入口 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 13 | deep link scheme完整配置 | invite_screen已有，需验证iOS Universal Link/Android App Link | 裂变归因准确 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 14 | 落地页用户评价区块 | PROMOTION_STRATEGY Week1 Action | 信任背书 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 15 | 新用户引导流程优化 | 当前Onboarding问卷缺少强制步骤 | 激活率 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 四、运营侧待执行事项
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### Phase 1（本周）：堵住泄漏
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 事项 | 负责人 | 验收标准 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|--------|----------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 18 | 抓Android真机崩溃日志，修复ASR闪退 | 技术 | 5台真机不闪退 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 19 | 邀请裂变端到端测试 | 技术+运营 | 注册→邀请→分享→注册→奖励全部正常 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 20 | PostHog手动验证 | 技术 | 数据面板可见权限快照和事件 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 21 | 截图9张 + 应用介绍100字 + 朋友圈文案10条 | 运营 | 物料就绪 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 22 | 建立第一个50人英语学习微信群 | 运营 | 群建立并开始日常运营 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### Phase 2（下周）：启动增长
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 事项 | 来源 | 验收标准 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|------|----------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 23 | 小红书日更（10篇笔记） | PROMOTION_STRATEGY Week1 | 阅读500+/篇 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 24 | 知乎回答100个英语相关问题 | PROMOTION_STRATEGY Week1 | 收藏50+/回答 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 25 | 联系10个大学英语社团 | PROMOTION_STRATEGY Week1 | 5个以上回复 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 26 | 联系现有C端教师用户做B端转化 | QUICK_CASH_PLAN Day1 | 转化5家意向 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 27 | 企查查搜索培训机构50家名单 | QUICK_CASH_PLAN Day2 | 名单整理完成 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 28 | 发起「30天跟读挑战」活动 | PROMOTION_STRATEGY | 活动页面可用 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |### Phase 3（2-4周）：规模化
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || # | 事项 | 来源 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||---|------|------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 29 | 组建专家团（5-7人） | 专家团组建方案 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 30 | B端销售首批10家机构触达 | QUICK_CASH_PLAN |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 31 | 代理商体系招募首批代理 | 代理商销售提成方案 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 32 | 教育科技展会参展 | B端推广方案 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 33 | KOL合作（小红书+抖音腰部） | C端推广方案 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 34 | 落地页优化（用户评价区块） | PROMOTION_STRATEGY |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 35 | App Store/Google Play提交审核 | ASO指南 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 五、关键风险
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |```
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |┌─────────────────────────────────────────────────────────────┐
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│ 🔴 Android离线ASR闪退                                        │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 无法获取真实设备崩溃日志                                 │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 建议：立即安排真机测试抓取logcat+tombstones             │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |├─────────────────────────────────────────────────────────────┤
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│ 🟡 邀请裂变后端闭环待验证                                    │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → API在3006端口可访问，gateway在3003端口代理               │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 需端到端测试（真机操作）确认全流程畅通                    │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |├─────────────────────────────────────────────────────────────┤
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│ 🟡 专家团已超期未启动                                        │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 计划Week 7启动，当前已过时间节点                         │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 建议：本周开始候选人名单和邀请函                          │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |├─────────────────────────────────────────────────────────────┤
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│ 🟢 文档体系完整，落地执行是短板                              │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 58个MD+42个PDF，但大部分动作未开始                       │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |│   → 建议：先修P0产品问题，再推运营                           │
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |└─────────────────────────────────────────────────────────────┘
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |```
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 六、执行顺序
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |```
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |Phase 1（本周）：堵住泄漏
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 1. 抓Android真机崩溃日志 → 修复ASR闪退
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 2. 验证邀请裂变端到端流程
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 3. 完成PostHog埋点手动验证
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |Phase 2（下周）：启动增长
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 4. 建立第一个微信学习群（50人）
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 5. 小红书/知乎开始内容输出
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 6. 截图+介绍文案物料准备
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |Phase 3（2-4周）：规模化
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 7. 专家团正式启动
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 8. B端机构首批触达
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 9. App Store/Google Play上架
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |  [ ] 10. 裂变活动正式上线
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |```
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |---
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |## 七、服务器状态
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || 服务 | 端口 | 状态 | 备注 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） ||------|------|------|------|
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || echo-transcribe（主API） | 3006 | ✅ 运行中 | PM2管理，invite/activation路由已实现 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || api-gateway | 3017 | ✅ 运行中 | 代理/ai/chat/companion等路由 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || landing page | 8100 | ✅ 运行中 | HTML静态服务 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） || echo-subscribe-api | - | ⚠️ 独立服务 | 邮件订阅专用 |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |
 邀请裂变（后端） | ✅ API验证通过 | 100%（待真机端到端） |
| PostHog 埋点链路 | ✅ 连接验证通过 | 100%（待真机数据确认） |> 注意：Flutter `apiBaseUrl` 默认 `http://localhost:3000`，release APK 通过 `--dart-define=API_BASE_URL=http://38.55.146.160:3017` 注入生产地址。invite 路由在 3006 端口，需确认 gateway 3017 是否正确代理到 3006。
